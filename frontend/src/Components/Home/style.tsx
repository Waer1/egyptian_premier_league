import { Box, styled } from "@mui/material";

export const Container = styled(Box)(() => ({
    // marginTop:15,
    height:"100%",
    marginLeft:10,
    paddingLeft:10,
    borderLeft:"1px solid #e0e0e0",
    }));